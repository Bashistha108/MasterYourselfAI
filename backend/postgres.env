# PostgreSQL Database Configuration (for Render)
# Render will automatically set DATABASE_URL, so you don't need these manually
POSTGRES_HOST=your-postgres-host.render.com
POSTGRES_PORT=5432
POSTGRES_USER=master_ai_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DATABASE=master_yourself_ai

# Email Configuration
FEEDBACK_EMAIL_ADDRESS=your_email@gmail.com
FEEDBACK_EMAIL_PASSWORD=your_app_password

# Flask Configuration
SECRET_KEY=your_secret_key_here

# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key
